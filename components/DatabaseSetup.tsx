
import React from 'react';
import { useLocalization } from '../hooks/useLocalization';
import { X, Copy, Database, Image as ImageIcon, Users, AlertTriangle, ShieldAlert, CheckCircle, Unlock, Bell, ShoppingBag, ListPlus, UserCheck, Megaphone, ShieldCheck, Layers, Zap, Tag, FileText, MessageSquare, ThumbsUp, BrainCircuit, Rocket, Wrench } from 'lucide-react';

const CodeBlock: React.FC<{ title: string; code: string }> = ({ title, code }) => {
  const { t } = useLocalization();
  const [copied, setCopied] = React.useState(false);

  const handleCopy = () => {
    navigator.clipboard.writeText(code);
    setCopied(true);
    setTimeout(() => setCopied(false), 2000);
  };

  return (
    <div className="mb-6">
      <h4 className="text-lg font-semibold text-dark dark:text-light mb-2">{title}</h4>
      <div className="relative bg-gray-200 dark:bg-gray-900 rounded-lg p-4 font-mono text-sm text-gray-800 dark:text-gray-300 overflow-x-auto">
        <button
          onClick={handleCopy}
          className="absolute top-2 right-2 p-1.5 bg-gray-300 dark:bg-gray-700 rounded-md hover:bg-gray-400 dark:hover:bg-gray-600"
          aria-label={t('copyCode')}
        >
          <Copy size={16} />
        </button>
        <pre><code>{code}</code></pre>
        {copied && <span className="absolute bottom-2 right-2 text-xs text-green-600 dark:text-green-400">{t('copied')}</span>}
      </div>
    </div>
  );
};


const DatabaseSetup: React.FC<{ isOpen: boolean; onClose: () => void; }> = ({ isOpen, onClose }) => {
  const { t } = useLocalization();
  
  // SQL FIX FOR V31: FORCE ADD SPONSORED COLUMNS
  const v31SQL = `-- V31: FORCE Fix Sponsored Columns
-- 1. Ensure provider_ads has the columns
ALTER TABLE public.provider_ads ADD COLUMN IF NOT EXISTS is_sponsored boolean DEFAULT false;
ALTER TABLE public.provider_ads ADD COLUMN IF NOT EXISTS sponsored_end_date timestamp with time zone;

-- 2. Ensure sponsored_requests exists
CREATE TABLE IF NOT EXISTS public.sponsored_requests (
  id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  provider_id bigint REFERENCES public.providers(id) ON DELETE CASCADE,
  ad_id bigint REFERENCES public.provider_ads(id) ON DELETE CASCADE,
  status text DEFAULT 'pending',
  created_at timestamp with time zone DEFAULT now()
);

-- 3. Reset RLS Policies for Ads (Fix Permission Denied)
ALTER TABLE public.provider_ads ENABLE ROW LEVEL SECURITY;
DROP POLICY IF EXISTS "Public Read Ads" ON public.provider_ads;
DROP POLICY IF EXISTS "Provider Manage Ads" ON public.provider_ads;
DROP POLICY IF EXISTS "Admin Manage Ads" ON public.provider_ads;

CREATE POLICY "Public Read Ads" ON public.provider_ads FOR SELECT USING (true);
CREATE POLICY "Provider Manage Ads" ON public.provider_ads FOR ALL USING (true) WITH CHECK (true);

-- 4. Reload Schema Cache
NOTIFY pgrst, 'reload config';`;

  // SQL FIX FOR V30: Feed Features (Likes, Comments, Sponsored Ads)
  const v30SQL = `-- V30: Feed Social Features & Sponsored Ads
ALTER TABLE public.provider_ads ADD COLUMN IF NOT EXISTS is_sponsored boolean DEFAULT false;
ALTER TABLE public.provider_ads ADD COLUMN IF NOT EXISTS sponsored_end_date timestamp with time zone;

CREATE TABLE IF NOT EXISTS public.ad_likes (
  id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  ad_id bigint REFERENCES public.provider_ads(id) ON DELETE CASCADE,
  user_id bigint REFERENCES public.clients(id) ON DELETE CASCADE,
  created_at timestamp with time zone DEFAULT now(),
  UNIQUE(ad_id, user_id)
);

CREATE TABLE IF NOT EXISTS public.ad_comments (
  id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  ad_id bigint REFERENCES public.provider_ads(id) ON DELETE CASCADE,
  user_id bigint REFERENCES public.clients(id) ON DELETE CASCADE,
  user_name text,
  comment text,
  created_at timestamp with time zone DEFAULT now()
);

CREATE TABLE IF NOT EXISTS public.sponsored_requests (
  id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  provider_id bigint REFERENCES public.providers(id) ON DELETE CASCADE,
  ad_id bigint REFERENCES public.provider_ads(id) ON DELETE CASCADE,
  status text DEFAULT 'pending', 
  created_at timestamp with time zone DEFAULT now()
);

ALTER TABLE public.ad_likes ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.ad_comments ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.sponsored_requests ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Public Read Likes" ON public.ad_likes FOR SELECT USING (true);
CREATE POLICY "Public Insert Likes" ON public.ad_likes FOR INSERT WITH CHECK (true);
CREATE POLICY "Public Delete Likes" ON public.ad_likes FOR DELETE USING (true);

CREATE POLICY "Public Read Comments" ON public.ad_comments FOR SELECT USING (true);
CREATE POLICY "Public Insert Comments" ON public.ad_comments FOR INSERT WITH CHECK (true);

CREATE POLICY "Public Read Requests" ON public.sponsored_requests FOR SELECT USING (true);
CREATE POLICY "Public Insert Requests" ON public.sponsored_requests FOR INSERT WITH CHECK (true);
CREATE POLICY "Public Update Requests" ON public.sponsored_requests FOR UPDATE USING (true);

NOTIFY pgrst, 'reload config';`;

  // SQL FOR V29: Duplicate Scan Prevention
  const v29SQL = `-- V29: Prevent Duplicate QR Scans
ALTER TABLE public.scan_history ADD COLUMN IF NOT EXISTS appointment_id bigint;
NOTIFY pgrst, 'reload config';`;

  const completeSQL = `-- Complete SQL Script
${v29SQL}
${v30SQL}
${v31SQL}
`;

  if (!isOpen) return null;

  return (
    <>
      <div className="fixed inset-0 bg-black/60 z-40" onClick={onClose}></div>
      <div className="fixed top-0 right-0 rtl:left-0 rtl:right-auto h-full w-full max-w-2xl bg-gray-100 dark:bg-gray-800 shadow-xl z-50 transform transition-transform duration-300 ease-in-out" style={{ transform: isOpen ? 'translateX(0)' : 'translateX(100%)' }}>
        <div className="p-4 flex justify-between items-center border-b border-gray-200 dark:border-gray-700 sticky top-0 bg-gray-100 dark:bg-gray-800 z-10">
          <h3 className="text-xl font-bold text-dark dark:text-light flex items-center gap-2">
            <Database /> {t('databaseSetupTitle')}
          </h3>
          <button onClick={onClose} className="p-2 rounded-full hover:bg-gray-200 dark:hover:bg-gray-600">
            <X size={24} className="text-dark dark:text-light" />
          </button>
        </div>
        <div className="p-6 h-[calc(100vh-65px)] overflow-y-auto">
          
          {/* V31 UPDATE BLOCK (NEW) */}
           <div className="mb-8 p-4 border-l-4 border-green-500 bg-green-50 dark:bg-green-900/20 rounded-r-lg shadow-lg animate-fade-in">
             <div className="flex items-center gap-2 mb-2">
                <Wrench className="text-green-600 dark:text-green-400" size={24}/>
                <h3 className="text-xl font-bold text-green-600 dark:text-green-400">
                   V31: إصلاح زر الموافقة (Approve Fix)
                </h3>
             </div>
             <p className="mb-3 text-sm text-gray-700 dark:text-gray-300 font-bold">
                شغل هذا الكود لإضافة عمود 'is_sponsored' وإصلاح المشكلة نهائياً.
             </p>
             <CodeBlock title="SQL V31 Code" code={v31SQL.trim()} />
          </div>

          {/* V30 UPDATE BLOCK */}
           <div className="mb-8 p-4 border-l-4 border-purple-500 bg-purple-50 dark:bg-purple-900/20 rounded-r-lg shadow-lg animate-fade-in">
             <div className="flex items-center gap-2 mb-2">
                <Rocket className="text-purple-600 dark:text-purple-400" size={24}/>
                <h3 className="text-xl font-bold text-purple-600 dark:text-purple-400">
                   V30: Feed & Sponsored Ads
                </h3>
             </div>
             <p className="mb-3 text-sm text-gray-700 dark:text-gray-300 font-bold">
                Run this to enable Instagram-style feed, likes, comments, and sponsored ads.
             </p>
             <CodeBlock title="SQL V30 Code" code={v30SQL.trim()} />
          </div>

          <p className="mb-6 text-gray-700 dark:text-gray-300">{t('databaseSetupDesc')}</p>
          <CodeBlock title={t('completeSQLScript')} code={completeSQL.trim()} />
        </div>
      </div>
    </>
  );
};

export default DatabaseSetup;
