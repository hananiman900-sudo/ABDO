
import React from 'react';
import { useLocalization } from '../hooks/useLocalization';
import { X, Copy, Database, Wrench } from 'lucide-react';

const CodeBlock: React.FC<{ title: string; code: string }> = ({ title, code }) => {
  const { t } = useLocalization();
  const [copied, setCopied] = React.useState(false);
  const handleCopy = () => { navigator.clipboard.writeText(code); setCopied(true); setTimeout(() => setCopied(false), 2000); };
  return (
    <div className="mb-6">
      <h4 className="text-lg font-semibold dark:text-light mb-2">{title}</h4>
      <div className="relative bg-gray-200 dark:bg-gray-900 rounded-lg p-4 font-mono text-sm overflow-x-auto">
        <button onClick={handleCopy} className="absolute top-2 right-2 p-1.5 bg-gray-300 rounded"><Copy size={16} /></button>
        <pre><code>{code}</code></pre>
        {copied && <span className="absolute bottom-2 right-2 text-xs text-green-600">{t('copied')}</span>}
      </div>
    </div>
  );
};

const DatabaseSetup: React.FC<{ isOpen: boolean; onClose: () => void; }> = ({ isOpen, onClose }) => {
  const { t } = useLocalization();
  
  const finalSQL = `
-- 1. AD REQUESTS TABLE
CREATE TABLE IF NOT EXISTS public.provider_ad_requests (
    id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    created_at timestamp with time zone DEFAULT now(),
    provider_id bigint REFERENCES public.providers(id),
    message text,
    image_url text,
    status text DEFAULT 'pending'
);
ALTER TABLE public.provider_ad_requests ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Public Ad Requests" ON public.provider_ad_requests FOR ALL USING (true) WITH CHECK (true);

-- 2. ENSURE ORDERS TABLE
CREATE TABLE IF NOT EXISTS public.orders (
    id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    created_at timestamp with time zone DEFAULT now(),
    user_id bigint,
    user_type text,
    total_amount numeric,
    status text DEFAULT 'pending',
    items jsonb,
    customer_details jsonb
);
ALTER TABLE public.orders ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Public Orders" ON public.orders FOR ALL USING (true) WITH CHECK (true);
  `;

  if (!isOpen) return null;

  return (
    <>
      <div className="fixed inset-0 bg-black/60 z-40" onClick={onClose}></div>
      <div className="fixed right-0 h-full w-full max-w-2xl bg-white dark:bg-gray-800 shadow-xl z-50 p-6 overflow-y-auto animate-slide-up">
          <div className="flex justify-between items-center mb-6">
              <h3 className="text-xl font-bold dark:text-white flex gap-2"><Database/> Database Setup</h3>
              <button onClick={onClose}><X/></button>
          </div>
          <CodeBlock title="Final Update SQL" code={finalSQL.trim()} />
      </div>
    </>
  );
};

export default DatabaseSetup;