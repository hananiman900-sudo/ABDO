
import React from 'react';
import { useLocalization } from '../hooks/useLocalization';
import { X, Copy, Database, Image as ImageIcon, Users, AlertTriangle, ShieldAlert, CheckCircle, Unlock, Bell, ShoppingBag, ListPlus, UserCheck, Megaphone, ShieldCheck, Layers, Zap, Tag, FileText, MessageSquare, ThumbsUp } from 'lucide-react';

const CodeBlock: React.FC<{ title: string; code: string }> = ({ title, code }) => {
  const { t } = useLocalization();
  const [copied, setCopied] = React.useState(false);

  const handleCopy = () => {
    navigator.clipboard.writeText(code);
    setCopied(true);
    setTimeout(() => setCopied(false), 2000);
  };

  return (
    <div className="mb-6">
      <h4 className="text-lg font-semibold text-dark dark:text-light mb-2">{title}</h4>
      <div className="relative bg-gray-200 dark:bg-gray-900 rounded-lg p-4 font-mono text-sm text-gray-800 dark:text-gray-300 overflow-x-auto">
        <button
          onClick={handleCopy}
          className="absolute top-2 right-2 p-1.5 bg-gray-300 dark:bg-gray-700 rounded-md hover:bg-gray-400 dark:hover:bg-gray-600"
          aria-label={t('copyCode')}
        >
          <Copy size={16} />
        </button>
        <pre><code>{code}</code></pre>
        {copied && <span className="absolute bottom-2 right-2 text-xs text-green-600 dark:text-green-400">{t('copied')}</span>}
      </div>
    </div>
  );
};


const DatabaseSetup: React.FC<{ isOpen: boolean; onClose: () => void; }> = ({ isOpen, onClose }) => {
  const { t } = useLocalization();
  
  // ... existing variables ...
  const clientsTableSQL = `CREATE TABLE IF NOT EXISTS public.clients (
  id bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  full_name text NULL,
  phone text NULL,
  password text NULL,
  CONSTRAINT clients_pkey PRIMARY KEY (id),
  CONSTRAINT clients_phone_key UNIQUE (phone)
) TABLESPACE pg_default;`;

  const providersTableSQL = `CREATE TABLE IF NOT EXISTS public.providers (
  id bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  name text NULL,
  service_type text NULL,
  location text NULL,
  username text NULL,
  password text NULL,
  phone text NULL,
  is_active boolean DEFAULT false,
  subscription_end_date timestamp with time zone NULL,
  latitude float NULL,
  longitude float NULL,
  bio text NULL,
  profile_image_url text NULL,
  CONSTRAINT providers_pkey PRIMARY KEY (id),
  CONSTRAINT providers_username_key UNIQUE (username)
) TABLESPACE pg_default;`;

  const appointmentsTableSQL = `CREATE TABLE IF NOT EXISTS public.appointments (
  id bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  client_id bigint NULL,
  provider_id bigint NULL,
  CONSTRAINT appointments_pkey PRIMARY KEY (id),
  CONSTRAINT appointments_client_id_fkey FOREIGN KEY (client_id) REFERENCES clients(id) ON DELETE CASCADE,
  CONSTRAINT appointments_provider_id_fkey FOREIGN KEY (provider_id) REFERENCES providers(id) ON DELETE CASCADE
) TABLESPACE pg_default;`;

  const followUpsTableSQL = `CREATE TABLE IF NOT EXISTS public.follow_ups (
  id bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  client_id bigint NULL,
  provider_id bigint NULL,
  next_appointment_date timestamp with time zone NULL,
  notes text NULL,
  medication_image_url text NULL,
  CONSTRAINT follow_ups_pkey PRIMARY KEY (id),
  CONSTRAINT follow_ups_client_id_fkey FOREIGN KEY (client_id) REFERENCES clients(id) ON DELETE CASCADE,
  CONSTRAINT follow_ups_provider_id_fkey FOREIGN KEY (provider_id) REFERENCES providers(id) ON DELETE CASCADE
) TABLESPACE pg_default;`;

  const announcementsTableSQL = `CREATE TABLE IF NOT EXISTS public.announcements (
  id bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  provider_id bigint NULL,
  message text NULL,
  CONSTRAINT announcements_pkey PRIMARY KEY (id),
  CONSTRAINT announcements_provider_id_fkey FOREIGN KEY (provider_id) REFERENCES providers(id) ON DELETE CASCADE
) TABLESPACE pg_default;`;

  const followsTableSQL = `CREATE TABLE IF NOT EXISTS public.follows (
  id bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  client_id bigint NULL,
  provider_id bigint NULL,
  CONSTRAINT follows_pkey PRIMARY KEY (id),
  CONSTRAINT follows_client_id_fkey FOREIGN KEY (client_id) REFERENCES clients(id) ON DELETE CASCADE,
  CONSTRAINT follows_provider_id_fkey FOREIGN KEY (provider_id) REFERENCES providers(id) ON DELETE CASCADE,
  CONSTRAINT follows_unique UNIQUE (client_id, provider_id)
) TABLESPACE pg_default;`;

  const scanHistoryTableSQL = `CREATE TABLE IF NOT EXISTS public.scan_history (
  id bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  provider_id bigint NULL,
  client_name text NULL,
  client_phone text NULL,
  CONSTRAINT scan_history_pkey PRIMARY KEY (id),
  CONSTRAINT scan_history_provider_id_fkey FOREIGN KEY (provider_id) REFERENCES providers(id) ON DELETE CASCADE
) TABLESPACE pg_default;`;

const providerServicesTableSQL = `CREATE TABLE IF NOT EXISTS public.provider_services (
  id bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  provider_id bigint NULL,
  name text NOT NULL,
  price numeric NULL,
  discount_price numeric NULL,
  CONSTRAINT provider_services_pkey PRIMARY KEY (id),
  CONSTRAINT provider_services_provider_id_fkey FOREIGN KEY (provider_id) REFERENCES providers(id) ON DELETE CASCADE
) TABLESPACE pg_default;`;

const v17UpdateSQL = `-- Update V17: Offers, Urgent Ads & Social
CREATE TABLE IF NOT EXISTS public.provider_offers (
  id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  created_at timestamp with time zone DEFAULT now(),
  provider_id bigint REFERENCES public.providers(id),
  title text,
  original_price numeric,
  discount_price numeric,
  image_url text,
  end_date timestamp with time zone
);
ALTER TABLE public.provider_offers ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Public Access Offers" ON public.provider_offers FOR ALL USING (true) WITH CHECK (true);

CREATE TABLE IF NOT EXISTS public.urgent_ads (
  id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  created_at timestamp with time zone DEFAULT now(),
  provider_id bigint REFERENCES public.providers(id),
  message text,
  is_active boolean DEFAULT true
);
ALTER TABLE public.urgent_ads ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Public Access Urgent" ON public.urgent_ads FOR ALL USING (true) WITH CHECK (true);

ALTER TABLE public.providers ADD COLUMN IF NOT EXISTS social_links jsonb DEFAULT '{}';
NOTIFY pgrst, 'reload config';`;

const v18UpdateSQL = `-- Update V18: Paid Ad Requests Table
CREATE TABLE IF NOT EXISTS public.provider_ad_requests (
  id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  created_at timestamp with time zone DEFAULT now(),
  provider_id bigint REFERENCES public.providers(id),
  message text,
  image_url text,
  status text DEFAULT 'pending'
);

ALTER TABLE public.provider_ad_requests ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Public Access AdRequests" ON public.provider_ad_requests FOR ALL USING (true) WITH CHECK (true);
NOTIFY pgrst, 'reload config';`;

const v20UpdateSQL = `-- Update V20: Product Multi-Images & Comments
ALTER TABLE public.products ADD COLUMN IF NOT EXISTS images text[] DEFAULT NULL;

CREATE TABLE IF NOT EXISTS public.product_reviews (
  id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  created_at timestamp with time zone DEFAULT now(),
  product_id bigint REFERENCES public.products(id) ON DELETE CASCADE,
  user_name text NOT NULL,
  comment text NOT NULL
);

ALTER TABLE public.product_reviews ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Public Access Reviews" ON public.product_reviews FOR ALL USING (true) WITH CHECK (true);

NOTIFY pgrst, 'reload config';`;

const v21UpdateSQL = `-- Update V21: Unique Likes Logic
CREATE TABLE IF NOT EXISTS public.job_likes (
  id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  created_at timestamp with time zone DEFAULT now(),
  job_id bigint REFERENCES public.job_posts(id) ON DELETE CASCADE,
  user_id bigint NOT NULL,
  CONSTRAINT job_likes_unique UNIQUE (job_id, user_id)
);

ALTER TABLE public.job_likes ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Public Access JobLikes" ON public.job_likes FOR ALL USING (true) WITH CHECK (true);

NOTIFY pgrst, 'reload config';`;

  return (
    isOpen ? (
      <div className="fixed inset-0 bg-black/50 z-50 flex items-center justify-center p-4">
          <div className="bg-white p-6 rounded-xl w-full max-w-lg relative h-[80vh] overflow-y-auto">
              <button onClick={onClose} className="absolute top-4 right-4"><X/></button>
              <h2 className="font-bold text-xl mb-4 flex items-center gap-2"><Database/> Setup SQL</h2>
              
              <div className="mb-8 p-4 border-l-4 border-teal-500 bg-teal-50 rounded-r-lg">
                 <h3 className="text-xl font-bold text-teal-600 flex items-center gap-2 mb-4"><ThumbsUp/> V21 Update (New)</h3>
                 <p className="text-sm text-gray-600 mb-2">Unique Likes Logic (One like per user)</p>
                 <CodeBlock title="Job Likes Table" code={v21UpdateSQL} />
              </div>

              <div className="mb-8 p-4 border-l-4 border-purple-500 bg-purple-50 rounded-r-lg">
                 <h3 className="text-xl font-bold text-purple-600 flex items-center gap-2 mb-4"><Zap/> V17 Update</h3>
                 <CodeBlock title="Offers, Social & Urgent Ads" code={v17UpdateSQL} />
              </div>

              <div className="mb-8 p-4 border-l-4 border-pink-500 bg-pink-50 rounded-r-lg">
                 <h3 className="text-xl font-bold text-pink-600 flex items-center gap-2 mb-4"><FileText/> V18 Update</h3>
                 <CodeBlock title="Ad Requests Table" code={v18UpdateSQL} />
              </div>

               <div className="mb-8 p-4 border-l-4 border-indigo-500 bg-indigo-50 rounded-r-lg">
                 <h3 className="text-xl font-bold text-indigo-600 flex items-center gap-2 mb-4"><MessageSquare/> V20 Update</h3>
                 <CodeBlock title="Product Upgrades" code={v20UpdateSQL} />
              </div>
          </div>
      </div>
    ) : null
  );
};
export default DatabaseSetup;
